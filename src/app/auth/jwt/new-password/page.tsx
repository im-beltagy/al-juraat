import { SupabaseNewPasswordView } from 'src/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Change Password | Al-Juraat Al-Tibbiya',
};

export default function LoginPage() {
  return <SupabaseNewPasswordView />;
}
