export const metadata = {
  title: 'Trade Names | Al-Juraat Al-Tibbiya',
};

type Props = {
  children: React.ReactNode;
};

export default function Layout({ children }: Props) {
  return children;
}
